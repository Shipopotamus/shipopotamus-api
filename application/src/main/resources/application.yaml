authentication:
  database:
    host: localhost
    port: 5432
    username: postgres
    password: postgres
    databaseName: shipopotamus
    schemaName: authentication_schema
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
